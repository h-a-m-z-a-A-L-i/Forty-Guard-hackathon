import React from 'react';
const value = (n, digits=1) => Number.isFinite(Number(n)) ? Number(n).toFixed(digits) : '—'; const mins=s=>Number.isFinite(Number(s))?`${Math.round(s/60)} min`:'—'; const miles=m=>value(Number(m)/1609.34);
export default function RouteComparisonCards({routes,coolestRouteId,feelsLike}) { return <div className="cards">{routes.map(r=><article className={r.routeId===coolestRouteId?'card cool':'card'} key={r.routeId}><h3>{r.routeId===coolestRouteId?'🌿 Coolest route':`Route ${r.routeId+1}`}</h3><p className="big">{value(r.avgTemp)}°C</p><p>{mins(r.durationSeconds)} · {miles(r.distanceMeters)} mi</p><p>Max {value(r.maxTemp)}°C</p><p>⚠ {r.hoursAboveThreshold == null ? 'Unavailable' : `${value(r.hoursAboveThreshold)} hrs above 35°C`}</p>{r.routeId===coolestRouteId&&feelsLike&&<p>Feels like {value(feelsLike.heat_index_celsius?.[0])}°C</p>}</article>)}</div>; }
